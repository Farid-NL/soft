name: soft
help: Personal Fedora installer
version: 0.5.0

commands:
  - name: list
    alias: ls
    help: List all programs and show whether they are installed or not

  - name: install
    alias: i
    help: Install given program
    args:
      - name: program
        required: true

examples:
- soft list
